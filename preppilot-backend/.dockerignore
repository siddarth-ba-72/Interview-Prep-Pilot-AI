# Host build output must not leak into the image - Gradle rebuilds everything inside it.
**/build/
**/bin/
**/.gradle/
**/out/
**/logs/
.idea/
*.iml

# Secrets are supplied at runtime via docker-compose `env_file`, never baked into an image.
**/.env
**/.env.*
!**/.env.example
