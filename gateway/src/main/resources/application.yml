spring:
  application:
    name: gateway
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "${FRONTEND_ORIGIN}"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true
      routes:
        - id: oauth2-routes
          uri: "${USER_SERVICE_URL}"
          predicates:
            - Path=/oauth2/**,/login/oauth2/**
        - id: auth-routes
          uri: "${USER_SERVICE_URL}"
          predicates:
            - Path=/api/v1/auth/**
        - id: user-routes
          uri: "${USER_SERVICE_URL}"
          predicates:
            - Path=/api/v1/users/**
        - id: topic-routes
          uri: "${TOPIC_SERVICE_URL}"
          predicates:
            - Path=/api/v1/topics/**,/api/v1/sessions/**,/api/v1/reports/**
        - id: ai-routes
          uri: "${AI_SERVICE_URL}"
          predicates:
            - Path=/api/v1/ai/**

jwt:
  secret: "${JWT_SECRET}"

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never

server:
  port: 8080
